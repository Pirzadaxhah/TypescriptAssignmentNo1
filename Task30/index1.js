"use strict";
const MyArray = ["Admin", "Maryam", "kalsoom", "Areeba", "Mehak"];
for (const MyArrays of MyArray) {
    if (MyArrays === "Admin") {
        console.log(`Hello ${MyArrays}would liketo see the status report`);
    }
    else {
        console.log(`Hello ${MyArrays} for logging in again`);
    }
    //  const element = MyArray[index];
}
